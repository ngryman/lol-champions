{
  "name": "lol-champions",
  "version": "0.3.4",
  "description": "Simplified, up-to-date, League of Legends champions list.",
  "author": "Nicolas Gryman <ngryman@gmail.com> (http://ngryman.sh)",
  "license": "MIT",
  "repository": "ngryman/lol-champions",
  "main": "champions.json",
  "engines": {
    "node": ">=4"
  },
  "files": [
    "champions.json"
  ],
  "scripts": {
    "lint": "eslint *.js",
    "spec": "babel-node test.js --presets es2015 | tap-spec",
    "test": "npm run lint -s && npm run spec -s",
    "watch": "onchange {,**/}*.js -- npm test -s",
    "build": "babel-node -e \"require('./fetch').default().then((champions) => require('fs').writeFileSync('champions.json', JSON.stringify(champions, 0, 2)))\" --presets es2015",
    "prebuild": "ln -nsf ../lib node_modules/lol-champions",
    "prepublish": "npm run build -s"
  },
  "keywords": [
    "lol",
    "league",
    "legends",
    "champions",
    "api"
  ],
  "dependencies": {
    "got": "^6.3.0",
    "object-values": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "blue-tape": "^0.2.0",
    "eslint": "^2.13.0",
    "eslint-config-ngryman": "^1.0.0",
    "onchange": "^2.5.0",
    "tap-spec": "^4.0.2"
  }
}
